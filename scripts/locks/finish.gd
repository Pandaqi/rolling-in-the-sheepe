extends "res://scripts/locks/lock_general.gd"

# This is a dummy script for consistency
# Because finishes are a "lock", they expect a lock module and the general lock functionality
# But it doesn't need or use it otherwise
